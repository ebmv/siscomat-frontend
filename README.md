# SISCOMAT Frontend

## Cómo generar la documentación

Desde la carpeta del proyecto cuya documentación quieras generar ejecuta el siguiente comando:

```
npx typedoc --entryPointStrategy expand ./src
```

La documentación se generará automáticamente en la carpeta /docs
